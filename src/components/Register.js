import React from 'react';

// Temporary placeholder matching the /register route until full auth UI is migrated.

const Register = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <h2>Register</h2>
      <p>
        This is a temporary placeholder for the registration screen. The full form and validation
        from `src/components/Register.js` has not been migrated into `apps/frontend` yet.
      </p>
    </div>
  );
};

export default Register;

