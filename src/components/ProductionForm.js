import React from 'react';

// Temporary placeholder for the Production form view.

const ProductionForm = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <h2>Production Form</h2>
      <p>
        This is a temporary placeholder for `ProductionForm`. The detailed production entry form
        from the legacy implementation has not been migrated into `apps/frontend` yet.
      </p>
    </div>
  );
};

export default ProductionForm;

