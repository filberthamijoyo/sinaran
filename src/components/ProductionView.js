import React from 'react';

// Temporary placeholder for the Production detail view.

const ProductionView = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <h2>Production Detail</h2>
      <p>
        This is a temporary placeholder for `ProductionView`. The detailed read-only view from
        the legacy implementation has not been migrated into `apps/frontend` yet.
      </p>
    </div>
  );
};

export default ProductionView;

