import React from 'react';

// Temporary placeholder for the Production list view.

const ProductionList = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <h2>Production Database</h2>
      <p>
        This is a temporary placeholder for `ProductionList`. The full production list, filters,
        and table UI from the legacy implementation have not been migrated into `apps/frontend` yet.
      </p>
    </div>
  );
};

export default ProductionList;

