import React from 'react';

// Temporary placeholder component to satisfy imports and allow the app to compile.
// The full implementation still exists in the legacy `src/components/AdminPanel.js`.

const AdminPanel = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <h2>Admin Panel</h2>
      <p>
        This is a temporary placeholder for `AdminPanel`. The full admin management UI has not
        been migrated into `apps/frontend` yet.
      </p>
    </div>
  );
};

export default AdminPanel;

