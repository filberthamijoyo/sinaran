import React from 'react';

// Temporary placeholder matching the /login route until full auth UI is migrated.

const Login = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <h2>Login</h2>
      <p>
        This is a temporary placeholder for the login screen. The detailed auth UI from
        `src/components/Login.js` has not been migrated into `apps/frontend` yet.
      </p>
    </div>
  );
};

export default Login;

