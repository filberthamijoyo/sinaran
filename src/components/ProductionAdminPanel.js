import React from 'react';

// Temporary placeholder for the Production admin panel (dropdown maintenance, etc.).

const ProductionAdminPanel = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <h2>Production Admin Panel</h2>
      <p>
        This is a temporary placeholder for `ProductionAdminPanel`. The full dropdown and metadata
        management UI from the legacy implementation has not been migrated into `apps/frontend` yet.
      </p>
    </div>
  );
};

export default ProductionAdminPanel;

