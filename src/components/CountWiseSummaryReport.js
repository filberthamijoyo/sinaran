import React from 'react';

// Temporary placeholder for the count-wise production summary report.

const CountWiseSummaryReport = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <h2>Count-wise Summary Report</h2>
      <p>
        This is a temporary placeholder for `CountWiseSummaryReport`. The full count-wise
        aggregation and reporting view from the legacy implementation has not been migrated
        into `apps/frontend` yet.
      </p>
    </div>
  );
};

export default CountWiseSummaryReport;

