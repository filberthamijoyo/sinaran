{
  "name": "@erp-sinaran/prisma",
  "version": "1.0.0",
  "private": true,
  "main": "./index.js",
  "types": "./index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "build:scripts": "tsc",
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "populate-letter-codes": "tsx populate-letter-codes.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "csv-parse": "^6.1.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "prisma": "^6.19.2"
  }
}
